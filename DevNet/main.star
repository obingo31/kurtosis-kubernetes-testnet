def run(plan, **kwargs):
    # Simple test package
    plan.print("Kurtosis package is working!")
    plan.print("Args received: " + str(kwargs))

    # For now, just verify the package runs
    # Observability setup would go here
